---
source_path: explanation/enterprise-sdk.mdx
source_sha: 01045dac263ccf0625076b35bfcfe25c4fe2c85e
title: "Stable 企业版 SDK"
description: "使用类型化的 @stablechain/enterprise SDK 从您的后端中继免 Gas 费和保证区块空间的交易。"
diataxis: "explanation"
---

# Stable 企业版 SDK

`@stablechain/enterprise` 是 Stable 企业交易通道的服务器端 TypeScript 客户端。它签署并中继两种特权交易: 免 Gas 费交易（由您赞助用户的 Gas 费），以及保障区块空间交易（在预留的企业通道中处理）。您可以为单个客户端启用其中一个或两个通道。

```ts
import { createStableEnterprise, stableTestnet } from "@stablechain/enterprise";
import { privateKeyToAccount } from "viem/accounts";

const enterprise = createStableEnterprise({
  chain: stableTestnet,
  gasWaiver: { account: privateKeyToAccount("0xYOUR_WAIVER_KEY") },
});

const { txHash } = await enterprise.gasWaiver.send(user, { to: token, data });
```

```text
txHash: 0x8f3a...2d41
```

此调用返回 `H_inner`，即用户交易的哈希值，而不是承载它的封装交易的哈希值。

## SDK 的作用

SDK 提供了三个功能，每个功能对应一个模块。每个功能都是可选且独立配置的，并且每个功能都有自己的签名账户，因此您可以使用单独的密钥。

- **免 Gas 费中继** (`gasWaiver`): 构建、签署和中继免 Gas 费交易。一个白名单豁免账户封装用户的零 Gas 交易，因此用户无需持有任何 USDT0 即可进行交易。请参阅[免 Gas 费](/cn/explanation/gas-waiver)。
- **保障区块空间** (`guaranteedBlock`): 通过企业版 RPC 网关中继交易，使其进入预留的企业版通道区块空间。与免 Gas 费不同，签名者支付自己的 Gas 费。请参阅[保障区块空间](/cn/explanation/guaranteed-blockspace)。
- **保障中继交易** (`guaranteedWaiver`): 两者结合。通过保障区块空间路由的免 Gas 费交易，因此用户无需余额，交易仍能进入企业版通道。

每个功能都提供相同的四种方法: `send` 和 `sendBatch` 用于 SDK 签名的托管路径，`relay` 和 `relayBatch` 用于非托管路径，用户在其他地方签名并将签名的十六进制数据交给您。

SDK 以 [`@stablechain/enterprise` ](https://www.npmjs.com/package/@stablechain/enterprise)的形式发布在 npm 上，并要求 `viem >= 2.0.0` 作为对等依赖。

## 访问

企业版 SDK 目前仅在 Stable 测试网上提供。

这两个通道都是受限的。免 Gas 费需要治理注册的豁免密钥，而保障区块空间需要企业版 RPC 网关 API 密钥。要进行集成，请[联系 Stable](https://discord.gg/stablexyz) 以获取访问权限。Stable 会提供您需要的豁免密钥和网关端点。

## 仅限服务器端

:::warning
企业版 SDK 是一个无状态的服务器端库，它使用私钥进行签名。切勿在浏览器中运行它。将豁免密钥和企业版 RPC 网关 URL 保留在您的后端。
:::

豁免密钥是一个白名单，治理注册的账户：任何持有它的人都可以根据您的策略赞助 Gas 费。企业版 RPC 网关 URL 嵌入了您的 API 密钥。将两者都视为秘密。为了将豁免密钥排除在进程之外，请使用 AWS KMS 等托管签名器对其进行备份。请参阅[签名密钥和托管](/cn/reference/enterprise-sdk#signing-keys-and-custody)。

## 何时使用它

当您运营后端并希望赞助用户的 Gas 费或为自己的流量保证包含时，请使用企业版 SDK。对于来自后端或浏览器的日常转账、桥接、互换和保险库收益，请使用通用的 [Stable SDK](/cn/explanation/sdk-overview)。

## 从这里开始

- [**免 Gas 费中继**](/cn/how-to/relay-with-gas-waiver): 赞助用户的 Gas 费，让他们无需持有 USDT0 即可进行交易。
- [**发送保障交易**](/cn/how-to/send-guaranteed-transactions): 在预留的企业版通道区块空间中处理交易。
- [**保障中继交易**](/cn/how-to/guaranteed-relayed-transactions): 结合两个通道：在企业版通道中处理免 Gas 费交易。
- [**企业版 SDK 参考**](/cn/reference/enterprise-sdk): 每个模块、方法、配置选项和错误类。

## 下一步推荐

- [**从 npm 安装**](https://www.npmjs.com/package/@stablechain/enterprise): 在 npmjs.com 上查看软件包并检查最新版本。
- [**免 Gas 费协议**](/cn/reference/gas-waiver-api): 交易格式、标记路由和治理控制。
- [**Stable SDK**](/cn/explanation/sdk-overview): 用于转账、桥接、互换和收益的通用客户端。
- [**连接到 Stable**](/cn/reference/connect): 测试网的链 ID、RPC 端点和浏览器。
