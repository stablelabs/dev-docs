---
source_path: how-to/self-hosted-gas-waiver.mdx
source_sha: 9954e27c1926c87e7fdb45e5c7543e56f6594250
title: "自托管 Gas 豁免"
description: "在 Stable 上运行您自己的 Gas 豁免。通过治理注册豁免地址，构建封装交易，并广播它们，而无需托管服务器。"
diataxis: "how-to"
---

# 自托管 Gas 豁免

自托管 Gas 豁免允许您操作自己的豁免基础设施，而不是使用托管的豁免服务器 API。您通过链上治理注册一个豁免地址，然后直接向网络广播封装交易。

本指南涵盖了注册豁免地址、收集已签名的用户交易、构建封装交易以及广播它们。

:::note
**概念：** 有关 Gas 豁免是什么以及为什么存在，请参阅[Gas 豁免](/cn/explanation/gas-waiver)。有关完整的协议规范（封装交易机制、授权、策略检查、执行语义、安全模型），请参阅[Gas 豁免协议](/cn/reference/gas-waiver-api)。
:::

有关托管的豁免服务器 API 集成路径，请参阅[启用免 Gas 交易](/cn/how-to/integrate-gas-waiver)。

:::tip
`@stablechain/enterprise` 在一次调用中针对相同的治理注册豁免完成了以下所有操作。如果您正在编写 TypeScript，请使用[使用 Gas 豁免进行中继](/cn/how-to/relay-with-gas-waiver)，而不是手动构建封装交易。
:::

## 先决条件

- 通过验证器治理在链上注册的豁免地址。
- 为您的目标合约配置了 `AllowedTarget` 策略。

## 概述

自托管流程：

1. **从用户那里收集已签名的 InnerTx**，其中 `gasPrice = 0`。
2. **构建 WrapperTx**：RLP 编码 InnerTx 并将其封装在发送到标记地址的交易中。
3. 通过 `eth_sendRawTransaction` **广播** WrapperTx。

## 步骤 1：收集用户的 InnerTx

用户签署一个 `gasPrice = 0` 的交易。`to` 地址和方法选择器必须与您的豁免的 `AllowedTarget` 策略匹配。

```typescript
// config.ts
export const CONFIG = {
  RPC_URL: "https://rpc.testnet.stable.xyz",
  CHAIN_ID: 2201, // 988 for mainnet
  MARKER_ADDRESS: "0x000000000000000000000000000000000000f333",
  USDT0_ADDRESS: "0x78Cf24370174180738C5B8E352B6D14c83a6c9A9",
};
```

```typescript
// collectInnerTx.ts
import { ethers } from "ethers";
import { CONFIG } from "./config";

const provider = new ethers.JsonRpcProvider(CONFIG.RPC_URL);

const usdt0 = new ethers.Contract(CONFIG.USDT0_ADDRESS, [
  "function transfer(address to, uint256 amount) returns (bool)"
], provider);

const callData = usdt0.interface.encodeFunctionData("transfer", [
  recipientAddress,
  ethers.parseUnits("0.01", 18)
]);

const gasEstimate = await provider.estimateGas({
  from: userWallet.address,
  to: CONFIG.USDT0_ADDRESS,
  data: callData,
});

const nonce = await provider.getTransactionCount(userWallet.address);

const innerTx = {
  to: CONFIG.USDT0_ADDRESS,
  data: callData,
  value: 0,
  gasPrice: 0,
  gasLimit: gasEstimate,
  nonce: nonce,
  chainId: CONFIG.CHAIN_ID,
};

const signedInnerTx = await userWallet.signTransaction(innerTx);
```

## 步骤 2：构建 WrapperTx

RLP 编码已签名的 InnerTx 并将其封装在发送到标记地址的交易中。`gasLimit` 必须覆盖内部执行和封装开销。

```typescript
// constructWrapper.ts
import { ethers } from "ethers";
import { CONFIG } from "./config";

const innerTxBytes = ethers.decodeRlp(signedInnerTx);
const rlpEncoded = ethers.encodeRlp(innerTxBytes);

const waiverNonce = await provider.getTransactionCount(waiverWallet.address);

const wrapperTx = {
  to: CONFIG.MARKER_ADDRESS,
  data: rlpEncoded,
  value: 0,
  gasPrice: 0,
  gasLimit: (gasEstimate * 12n / 10n) * 2n,  // ~2x inner gas for overhead
  nonce: waiverNonce,
  chainId: CONFIG.CHAIN_ID,
};

const signedWrapperTx = await waiverWallet.signTransaction(wrapperTx);
```

:::warning
`InnerTx.gasPrice` 和 `WrapperTx.gasPrice` 都必须为 `0`。`WrapperTx.value` 也必须为 `0`。如果这些条件中的任何一个不满足，验证器将拒绝该交易。
:::

## 步骤 3：广播

通过标准 JSON-RPC 提交已签名的 WrapperTx。

```typescript
// broadcast.ts
const txHash = await provider.send("eth_sendRawTransaction", [signedWrapperTx]);
console.log("Wrapper tx broadcast:", txHash);

const receipt = await provider.waitForTransaction(txHash);
console.log("Confirmed:", receipt.status === 1);
```

```text
Wrapper tx broadcast: 0x...
Confirmed: true
```

## 主要收获

- 自托管豁免需要通过链上验证器治理注册的豁免地址。
- WrapperTx 被发送到标记地址 (`0x...f333`)，其中 RLP 编码的 InnerTx 作为数据。
- InnerTx 和 WrapperTx 都必须具有 `gasPrice = 0` 和 `value = 0`。

## 下一步建议

- [**Gas 豁免概念**](/cn/explanation/gas-waiver)：在您自己运行之前了解其机制。
- [**Gas 豁免协议**](/cn/reference/gas-waiver-api)：参考完整的协议规范，了解标记路由、授权和执行语义。
- [**启用免 Gas 交易**](/cn/how-to/integrate-gas-waiver)：使用托管的豁免服务器 API 而不是自托管。
